package apvd.lib.ellipse

/*
case class Point(e: Ellipse,
                 theta: Double)
*/

/*
case class Intersection(p1: Point,
                        p2: Point)

case class Intersections(e1: Ellipses, e2: Ellipses, intersections: Seq[Intersection])

object Intersections {
  def apply(e1: Ellipse, e2: Ellipse): Intersections =
    Intersections(
      e1,
      e2,
      {
        e1.intersect(e2)
      }
    )
}
*/

object Intersections {

}
